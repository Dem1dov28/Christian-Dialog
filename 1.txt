# =============================================================================
# ЛОКАЛЬНЫЙ ЗАПУСК (на вашем компьютере)
# =============================================================================
# ⚠️ ВАЖНО: Используйте .env.local для локальной разработки!
# .env — для production (на сервере)
# .env.local — для локальной разработки (автоматически загружается с приоритетом)

# Терминал 1: Backend
cd backend
py main.py

# Терминал 2: Frontend
cd frontend
npm run dev

cd backend
python scripts/sync_agents_from_config.py

ssh root@85.239.53.134

aK-i-XWuMBnJ1o

cd "/root/Epochal Dialog"

